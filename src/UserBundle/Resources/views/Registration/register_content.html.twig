{% trans_default_domain 'FOSUserBundle' %}
{% block stylesheet %}
    <link rel="stylesheet" href="{{ asset('login/style.css') }}">
{% endblock %}
{{ form_start(form, {'method': 'post' ,  'action': path('fos_user_registration_register'), 'attr': {'class': 'box'}}) }}
    {{ form_widget(form) }}
<div>
    <input type="submit" value="{{ 'registration.submit'|trans }}" />
</div>
{{ form_end(form) }}
