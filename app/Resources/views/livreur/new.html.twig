{% extends 'back.html.twig' %}

{% block main %}
    <h1>Livreur creation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('livreur_affich') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
